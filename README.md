# TASK 5: Kubernetes Cluster with Minikube

## Steps Followed:
1. Installed Minikube, kubectl, Docker
2. Started Minikube cluster
3. Deployed nginx app using deployment.yaml
4. Exposed app using NodePort in service.yaml
5. Verified pods and services via kubectl
6. Scaled deployment from 2 to 3 replicas
7. Described resources for status

## Screenshots:
Included in `/screenshots/` folder.

## Commands Used:
- `minikube start`
- `kubectl apply -f ...`
- `kubectl get pods/services`
- `kubectl scale`
- `kubectl describe`
